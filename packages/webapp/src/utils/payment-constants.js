const CREATED = 'created'
const STARTED = 'started'
const SUBMITTED = 'submitted'
const CAPTURABLE = 'capturable'
const SUCCESS = 'success'
const FAILED = 'failed'
const CANCELLED = 'cancelled'
const ERROR = 'error'

export default {
  paymentStatus: {
    CREATED,
    STARTED,
    SUBMITTED,
    CAPTURABLE,
    SUCCESS,
    FAILED,
    CANCELLED,
    ERROR
  }
}
